rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Universal access for any authenticated user (including anonymous)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
    
    // Deployment trigger version: v2.1.0 - Force rule propagation
  }
}