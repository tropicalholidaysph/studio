rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Tropical Ledger - Universal access for authenticated users
    // This allows both manual entry and bulk import to work correctly
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}