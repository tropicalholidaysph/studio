rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow full access to any authenticated user for all collections
    // This ensures that anonymous sign-in (used for the prototype) has full clearance
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}